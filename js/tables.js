/* =========================================
   Tables System
========================================= */


/* =========================
ACADEMIC TABLES
========================= */

const comparisonTables =
document.querySelectorAll(
  ".comparison-table"
);

if(comparisonTables.length){

  console.log(
    "Academic tables loaded"
  );

}
